# scripts/

This directory contains user scripts that will be JIT-compiled and executed by the engine.
Place webapps under `webapps/`, triggers under `triggers/` and shared libraries under `libraries/`.
